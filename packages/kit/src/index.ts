/**
 * tma-kit — runtime helpers for Telegram Mini Apps. Ships to production.
 *
 * Opinionated layers the SDK deliberately leaves out: auth, viewport/safe-area,
 * storage, payments, navigation, feature-gating. Everything tree-shakeable.
 */
export const KIT_VERSION = '0.0.0';
